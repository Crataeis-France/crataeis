/**
 * App-wide constants (imported from app code and e2e tests).
 */

/** `localStorage` key for JSON consent state — must stay in sync with `src/lib/cookie-consent.ts` usage. */
export const COOKIE_CONSENT_STORAGE_KEY = "crataeis_cookie_consent" as const;
